@using Avd4.Aula.Domain.Entidades
@model Inscricao
@{
    ViewData["Title"] = "Excluir inscrição";
}

<div class="row mt-3 mb-3">
    <div class="col-12">
        <fieldset>
            <legend>Excluir inscrição</legend>
        </fieldset>
        <hr />
    </div>
    <div class="col-md-12">
        <div class="alert alert-danger" role="alert">
            <h4 class="alert-heading">Excluir inscrição</h4>
            <p>Tem certeza que deseja excluir esta inscrição?</p>
            <hr>

            <form method="post" action="/admin-inscricao/excluir/@Model.Id">
                <button type="submit" class="btn btn-danger">Confirmar exclusão</button>
                <a href="/admin-inscricao/listar" class="btn btn-warning">Voltar para mimha lista de cursos</a>
            </form>
        </div>
    </div>

    <div class="col-md-12">
        @if (TempData["sucesso"] != null)
        {
            <div class="alert alert-info" role="alert">
                @TempData["sucesso"]
            </div>
        }
    </div>
</div>